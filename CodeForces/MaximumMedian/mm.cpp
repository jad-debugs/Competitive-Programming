#include <iostream>
#include <algorithm>
using namespace std;

#define ll long long

int n, k
const int hi = 2e9;
int arr[hi];

int works() 
{
     
}

bool findHalf() 
{
    for(int i = hi; i >= 1; i/=2) {
        if(works())
    }
} 

int main()
{
    cin >> n >> k;

    for(int i = 0; i < n; i++)
        cin >> arr[i];
    
    sort(arr, arr+n); 

    for(int i = 0; i < n; i++) {
          
    }

    return 0;
}
